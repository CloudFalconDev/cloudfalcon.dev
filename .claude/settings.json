{
	"hooks": {
		"PostToolUse": [
			{
				"matcher": "Write|Edit",
				"hooks": [
					{
						"type": "command",
						"command": "file=$(jq -r '.tool_input.file_path // .tool_input.path') && echo \"$file\" | grep -qE '\\.(ts|tsx|js|jsx|json)$' && npx biome check --write \"$file\" 2>/dev/null || exit 0"
					}
				]
			}
		],
		"Stop": [
			{
				"hooks": [
					{
						"type": "command",
						"command": "npm run build 2>&1 || (echo 'BUILD FAILED - please fix before continuing' && exit 2)"
					}
				]
			}
		]
	}
}
